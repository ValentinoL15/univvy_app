package com.unnivy.unnivy_app.dto.CommentsDTOs;

public record EditCommentDTO(Long comment_id,
                             String description,
                             Integer value
                             ) {
}
